<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.each do |attr,msg| %>
      <%= attr=='name' %>
        <li><%= if attr.to_s=='name' then 'Name' else attr end %> <%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :dob %><br>
    <%= f.date_select :dob %>
  </div>
  <div class="field">
    <%= f.label :male %><br>
    <%= f.number_field :male %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.collection_select(:role_id, Role.all, :id, :desc) %>

  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
